//bind object array together, when clicked all of them, return some value!import Phaser from '../lib/phaser.js'

export class Combination extends ??{
    
    constructor(scene,arr){
        super(scene,arr)
        scene.add.existing(this)
    }
    
}